# `lantern/` — R Package

R package for ancestry-specific rare variant association analysis with a C backend. v0.1.1, License MIT.

## LAYOUT

```
lantern/
├── DESCRIPTION / NAMESPACE        # package metadata
├── R/
│   ├── ancestry_split.R           # Step 1: ancestry_split(), split_diploid()/split_haplotype() C wrappers, read_bed_file()
│   ├── global_ancestry.R          # GLA shrinkage: .assign_arm(), .compute_arm_gla()
│   ├── write_ancestry_gds.R       # Step 2: write_ancestry_gds(), write_dosage_gds()
│   ├── ancestry_smmat.R           # Step 3: ancestry_smmat() (SMMAT + per-gene ancestry weights + Cauchy combination)
│   ├── utils.R                    # cauchy_combine()
│   ├── lantern-package.R          # package-level roxygen doc
│   └── sysdata.rda                # internal data: centromeres_hg38 (from data-raw/centromeres_hg38.R)
├── src/                           # C backend — see lantern/src/AGENTS.md
│   ├── ancestry.c                 # ALL core algorithms: p1/p2 split, phased split, bed reader
│   ├── ancestry.h                 # C function declarations
│   ├── init.c                     # R_CallMethodDef registration
│   └── Makevars                   # PKG_CFLAGS = -O3 -march=native -Wall
└── tests/testthat/
    ├── test-ancestry_split.R      # split_diploid/split_diploid_multi/split_haplotype + ancestry_split()
    ├── test-global_ancestry.R     # GLA shrinkage (.assign_arm, .compute_arm_gla, weighted blend)
    ├── test-ancestry_smmat.R      # ancestry_smmat() + cauchy_combine()
    ├── test-write_ancestry_gds.R  # write_dosage_gds()/write_ancestry_gds()
    └── test-utils.R
```

## EXPORTED R FUNCTIONS

| Function | Defined in | Wraps C entry |
|----------|-----------|---------------|
| `ancestry_split(vcf_path, msp_path, mode, chrom, ...)` | `R/ancestry_split.R` | (R-only; calls `split_diploid_multi`/`split_haplotype_multi`) |
| `ancestry_split_dosage(...)` / `ancestry_split_phased(...)` | `R/ancestry_split.R` | backward-compatible 2-population wrappers around `ancestry_split()` |
| `split_diploid(gt, ancestry, gla, arm_id)` | `R/ancestry_split.R` | `split_by_ancestry_C` |
| `split_diploid_multi(gt, ancestry, pure_codes, mixed_codes, gla, arm_id)` | `R/ancestry_split.R` | `split_by_ancestry_multi_C` |
| `split_haplotype(...)` / `split_haplotype_multi(...)` | `R/ancestry_split.R` | `split_phased_by_ancestry_C` / `split_phased_multi_C` |
| `read_bed_file(bed, bim, fam)` | `R/ancestry_split.R` | `read_bed_file_C` |
| `write_ancestry_gds(split_result, out_path, ...)` / `write_dosage_gds(...)` | `R/write_ancestry_gds.R` | (R-only; writes GDS via a temp VCF + `SeqArray::seqVCF2GDS`) |
| `ancestry_smmat(gds_paths, pheno, formula, kinship, gene_group_file, ...)` | `R/ancestry_smmat.R` | (R-only; wraps `GMMAT::glmmkin()`/`GMMAT::SMMAT()`) |
| `cauchy_combine(p_values, weights)` | `R/utils.R` | (R-only) |

`gla`/`arm_id` on `split_diploid()`/`split_diploid_multi()` are optional; `NULL` (the default) disables GLA shrinkage and reproduces the pre-shrinkage 0.5/0.5 singleton-fallback estimator exactly. See root `CLAUDE.md`'s "Core Algorithm" section for the shrinkage formula and rationale.

## BUILD & TEST

Always go through pixi (system gcc 9.3 breaks R 4.5 C23 enum syntax):

```bash
pixi run install-lantern          # R CMD INSTALL lantern/
pixi run test                     # devtools::test("lantern")
```

If the install fails with `Rboolean` errors, your `~/.R/Makevars` is being used instead of `R.Makevars.local`. Confirm `R_MAKEVARS_USER=$PWD/R.Makevars.local` is set in the task.

## CONVENTIONS (package-specific)

- **C entry registration**: every C function listed in `init.c` MUST also have a matching declaration in `ancestry.h` AND a non-`static` definition in `ancestry.c`. Mismatch causes linker/compiler errors.
- **R exports**: add an `@export` roxygen tag to the relevant `R/*.R` file, then re-run roxygen (`devtools::document("lantern")`). Don't hand-edit `NAMESPACE` (it says "Generated by roxygen2: do not edit by hand").
- **Matrix types**: genotype and PT matrices must be `integer` storage. R wrappers coerce via `storage.mode() <- "integer"`.
- **Ancestry codes**: integer 1/2/3 (2-population) or 1..K/mixed (K-population), never strings or factors.
- **No dead C entry points**: every function registered in `init.c` should have a real R-level caller somewhere in `R/*.R`. (Two functions violating this — `write_vcf_with_ancestry_C`, `subset_vcf_by_range_C` — were removed entirely in July 2026 after being found unreachable from any R code.)

See root `CLAUDE.md` for the full pipeline architecture, the GLA shrinkage algorithm, and current known issues (that file's "Known Issues" section is the source of truth — don't duplicate it here).
