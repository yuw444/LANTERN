% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/global_ancestry.R
\name{.compute_arm_gla}
\alias{.compute_arm_gla}
\title{Compute per-arm, per-population global local ancestry (GLA) proportions}
\usage{
.compute_arm_gla(tract_df, anc_diploid_tract, pure_codes_named, mixed_codes_df)
}
\arguments{
\item{tract_df}{data.frame with columns \code{chrom, spos, epos} (as
returned by \code{.parse_msp()}'s \code{tract_df}, already restricted to
the chromosome(s) being processed).}

\item{anc_diploid_tract}{Integer matrix (samples x tracts) of diploid
ancestry codes, same column order as \code{tract_df} rows.}

\item{pure_codes_named}{Named integer vector, population name -> diploid
code 1..K (as built in \code{\link{ancestry_split}}).}

\item{mixed_codes_df}{data.frame(code, pop1, pop2) for all mixed pairs (as
built in \code{\link{ancestry_split}}).}
}
\value{
Named list, one entry per chromosome present in \code{tract_df}
  (name = \code{"chr<N>"}), each a 2 x K numeric matrix (rows "p"/"q",
  columns = population names) of GLA proportions.
}
\description{
Aggregates the diploid ancestry code (1..K pure, K+1..K+M mixed) of every
sample over every MSP tract, length-weighted by tract bp, split into
p-arm/q-arm using \code{\link{.assign_arm}}. Falls back to whole-chromosome
GLA for an arm with zero informative tracts (e.g. the acrocentric p-arms of
chr13/14/15/21/22).
}
\keyword{internal}
